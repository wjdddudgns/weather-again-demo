import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode> 
);

/*
  리엑트라는 객체를 가져옴 
  레익토 돔이라는 객체를 가져옴
  index.css 파일을 가져옴
  App.js파일에서 App컴포넌트를 가져옴
*/